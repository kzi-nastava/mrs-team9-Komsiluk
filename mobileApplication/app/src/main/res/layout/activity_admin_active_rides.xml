<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/activeRidesRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <TextView
        android:id="@+id/tvActiveRidesTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="Live Active Rides"
        android:textColor="@color/secondary"
        android:textSize="@dimen/fs_h1"
        android:fontFamily="@font/roboto_serif_semibold" />

    <EditText
        android:id="@+id/etDriverSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:background="@android:color/white"
        android:hint="Search driver by name..."
        android:padding="12dp"
        android:textColor="@android:color/black"
        android:textColorHint="@android:color/darker_gray"
        android:textSize="@dimen/fs_md"
        android:fontFamily="@font/roboto_serif_medium"
        android:maxLines="1"
        android:inputType="textPersonName" />

    <TextView
        android:id="@+id/tvActiveRidesEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="12dp"
        android:text="No active rides at the moment."
        android:textColor="@color/white"
        android:textSize="@dimen/fs_md"
        android:fontFamily="@font/roboto_serif_medium"
        android:visibility="gone" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvActiveRides"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_marginTop="12dp" />

</LinearLayout>